{
  "name": "tactix",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "migrate": "pnpm -r migrate",
    "dev": "pnpm -r dev"
  },
  "engines": {
    "node": ">=20"
  }
}
