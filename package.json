{
  "name": "tactix",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "test:ci": "pnpm -r test",
    "migrate": "pnpm -r migrate"
  },
  "packageManager": "pnpm@9.0.0"
}
